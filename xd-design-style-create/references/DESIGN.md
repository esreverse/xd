# DESIGN.md

> Generated by xd-design-tokens-create
> DESIGN.md provides a baseline set of design tokens plus design decisions and best practices that cannot be captured as tokens. When the DTCG JSON files (core.tokens.json, semantic.tokens.json, …) are available and consumable, use them as the authoritative source for token values and follow this document for everything beyond tokens. When JSON consumption is not possible (e.g. tools without JSON support), DESIGN.md alone is sufficient — every token value is stated explicitly. The non-token guidance always applies, regardless of which source is used.



## 0. Context

[Style purpose and project context. Describes what this style is for, who the client is, what will be designed (website, app, prototype), and what makes this style distinctive. Max 320 characters in the tool — can be expanded here.]



## 1. Visual Theme & Atmosphere

Design decisions that go beyond what design tokens can capture. Only selected decisions are listed — unset parameters are left to the designer's judgement.


### Atmosphere

- **Default Theme:** Auto / Adaptive — The interface supports both light and dark themes, switching automatically based on system preference.
- **Background:** Mix — Page and section backgrounds mix neutral foundation colors with brand or accent colors where appropriate.
- **Whitespace:** Medium — Moderate whitespace between page sections and component groups. Balanced breathing room.
- **Positioning:** Grid mostly — Content follows a grid with occasional free placement for visual emphasis.
- **Elevation:** Dropshadows — The interface uses drop shadows to create visual depth and layered hierarchy.
- **Typography:** Functional — Typography serves readability and structure. Headings create hierarchy but stay systematic — no decorative or oversized type treatments.
- **Decoration:** Supportive — Decorative elements that support orientation and hierarchy — icons for wayfinding, shapes to emphasize structure, visual cues that guide the user through flows and funnels.
- **Vibe:** Casual — Relaxed, approachable, friendly. Warm but not playful — the interface feels human without being loud.
- **Complexity:** Moderate — Screens can contain a moderate amount of functionality. Filters, sorting, and secondary actions are acceptable when the use case calls for them.


### Surfaces

- **Style:** Solids — Surfaces use flat solid fills. No gradients or mesh effects.
- **Intensity:** Mixed — Surface colors vary between subtle and vivid depending on context.
- **Color:** Mixed — Surfaces mix neutral tones with selective use of brand or accent colors.
- **Colorfulness:** One color — One brand or accent color is used alongside neutrals.


### Inputs & Cards

- **Shapes:** Solid — Inputs and cards use solid filled shapes.
- **Edges:** Hairline — Inputs and cards have thin 1px borders.
- **Corners:** Soft — Inputs and cards have gently rounded corners.



## 2. Color Palette & Roles

Semantic color assignments with hex values. All colors reference `semantic.tokens.json` roles. Light mode values shown; dark mode inverts foundation pairs automatically.


### Foundation

| Token | Role | Light | Dark |
|-------|------|-------|------|
| `foundation.themed.base-clear` | Clear light/dark backgrounds | `#FAFAF8` | `#141412` |
| `foundation.themed.base-cloudy` | Cloudy light/dark backgrounds | `#F0EFE9` | `#1E1D1A` |
| `foundation.themed.base-higher` | Elevated surfaces | `#FAFAF8` | `#1E1D1A` |
| `foundation.themed.base-lower` | Background, base surfaces | `#F0EFE9` | `#141412` |
| `foundation.themed.contrast-primary` | Primary text, headings | `#141412` | `#FAFAF8` |
| `foundation.themed.contrast-secondary` | Secondary text, labels | `rgba(20,20,18,0.62)` | `rgba(250,250,248,0.62)` |
| `foundation.themed.contrast-tertiary` | Placeholder text, disabled | `rgba(20,20,18,0.40)` | `rgba(250,250,248,0.40)` |
| `foundation.themed.focus` | Focus rings | `#3B82F6` | `#60A5FA` |
| `foundation.themed.dash` | Dividers, borders | `rgba(20,20,18,0.15)` | `rgba(250,250,248,0.15)` |
| `foundation.themed.base-shine` | Subtle highlight overlay | `rgba(250,250,248,0.10)` | `rgba(250,250,248,0.10)` |
| `foundation.themed.base-shade` | Subtle shadow overlay | `rgba(20,20,18,0.10)` | `rgba(20,20,18,0.10)` |


### Foundation (Inverted)

Inverted tokens swap the theme — light tokens resolve to dark values and vice versa. Used for contrast sections, tooltips, and inverted UI elements.

| Token | Role | Light | Dark |
|-------|------|-------|------|
| `foundation.inverted.base-clear` | Clear dark/light backgrounds | `#141412` | `#FAFAF8` |
| `foundation.inverted.base-cloudy` | Cloudy dark/light backgrounds | `#1E1D1A` | `#F0EFE9` |
| `foundation.inverted.contrast-primary` | Primary text on inverted background | `#FAFAF8` | `#141412` |
| `foundation.inverted.contrast-secondary` | Secondary text on inverted background | `rgba(250,250,248,0.62)` | `rgba(20,20,18,0.62)` |


### Brand Colors

Each brand color generates a full OKLCH 11-step scale (50–950) in `core.tokens.json`. Semantic tokens reference the scale step closest to the defined hex. Every brand color exposes the same set of themed roles listed below. Inverted variants swap base and contrast roles for dark-on-light vs. light-on-dark contexts.

**Roles per brand color** (applies to `brand-a.themed.*`, `brand-b.themed.*`, …):

| Token | Use |
|-------|-----|
| `base` | Brand color base |
| `contrast-primary` | Primary text on brand color |
| `contrast-secondary` | Secondary text on brand color |
| `with-light-primary` | Primary brand color on light foundation |
| `with-light-secondary` | Secondary brand color on light foundation |
| `with-dark-primary` | Primary brand color on dark foundation |
| `with-dark-secondary` | Secondary brand color on dark foundation |
| `surface` | Light brand color as background |
| `surface-contrast` | Dark brand color on surface |

**Named brand colors:**

| Token | Name | Hex | Scale Step |
|-------|------|-----|------------|
| `brand-a.themed.base` | Steel Blue | `#465F76` | 500 |
| `brand-b.themed.base` | Coral | `#EC8272` | 500 |


### Accent Colors

Optional additional colors beyond the brand palette. Same OKLCH scale generation and themed/inverted structure as brand colors. Every accent color exposes the same role set as brand colors.

**Roles per accent color** (applies to `accent-a.themed.*`, `accent-b.themed.*`, …):

| Token | Use |
|-------|-----|
| `base` | Accent color base |
| `contrast-primary` | Primary text on accent color |
| `contrast-secondary` | Secondary text on accent color |
| `with-light-primary` | Primary accent color on light foundation |
| `with-light-secondary` | Secondary accent color on light foundation |
| `with-dark-primary` | Primary accent color on dark foundation |
| `with-dark-secondary` | Secondary accent color on dark foundation |
| `surface` | Light accent color as background |
| `surface-contrast` | Dark accent color on surface |


### Feedback Colors

Every feedback color (`info`, `success`, `warning`, `error`) exposes the same role set as brand/accent colors.

**Roles per feedback color** (applies to `feedback.info.themed.*`, `feedback.success.themed.*`, `feedback.warning.themed.*`, `feedback.error.themed.*`):

| Token | Use |
|-------|-----|
| `base` | Feedback color base |
| `contrast-primary` | Primary text on feedback color |
| `contrast-secondary` | Secondary text on feedback color |
| `with-light-primary` | Primary feedback color on light foundation |
| `with-light-secondary` | Secondary feedback color on light foundation |
| `with-dark-primary` | Primary feedback color on dark foundation |
| `with-dark-secondary` | Secondary feedback color on dark foundation |
| `surface` | Light feedback color as background |
| `surface-contrast` | Dark feedback color on surface |

**Named feedback colors:**

| Token | Name | Hex |
|-------|------|-----|
| `feedback.info.themed.base` | Info Blue | `#3B82F6` |
| `feedback.success.themed.base` | Success Green | `#22C55E` |
| `feedback.warning.themed.base` | Warning Amber | `#EAB308` |
| `feedback.error.themed.base` | Error Red | `#EF4444` |


### Action Colors

| Token | Role | Value |
|-------|------|-------|
| `action.primary.themed.base` | Primary action background | = `brand-a.themed.base` |
| `action.primary.themed.contrast` | Primary action text | = `brand-a.themed.contrast-primary` |
| `action.destructive.themed.base` | Destructive action background | = `feedback.error.themed.base` |
| `action.destructive.themed.contrast` | Destructive action text | = `feedback.error.themed.contrast-primary` |


### Combo Colors

Pre-configured color pairings for content sections. Each combo assigns a background source + foreground source with specific shade steps, and exposes the same role set as brand/accent/feedback colors so combos can be consumed uniformly.

**Roles per combo color** (applies to `combo-a.themed.*`, `combo-b.themed.*`, `combo-c.themed.*`, …):

| Token | Use |
|-------|-----|
| `base` | Combo color base |
| `contrast-primary` | Primary text on combo color |
| `contrast-secondary` | Secondary text on combo color |
| `with-light-primary` | Primary combo color on light foundation |
| `with-light-secondary` | Secondary combo color on light foundation |
| `with-dark-primary` | Primary combo color on dark foundation |
| `with-dark-secondary` | Secondary combo color on dark foundation |
| `surface` | Light combo color as background |
| `surface-contrast` | Dark combo color on surface |

**Configured combos:**

| Token | BG Source | FG Source | Use |
|-------|-----------|-----------|-----|
| `combo-a` | foundation | foundation | Default neutral sections |
| `combo-b` | brand-a | brand-a | Brand hero sections |
| `combo-c` | brand-b | brand-b | Accent feature sections |



## 3. Typography Rules

Font families, weights, and the complete type scale. All values reference `core.tokens.json` and `semantic.tokens.json`.


### Font Families

| Token | Stack | Use |
|-------|-------|-----|
| `core.font.family-sans` | `'Inter', -apple-system, sans-serif` | Headings (in sans-sans pairing) |
| `core.font.family-serif` | `'Playfair Display', Georgia, serif` | Headings (in serif-sans pairing) |
| `core.font.family-interface` | (= sans or serif per config) | UI elements, labels, body |
| `core.font.family-mono` | `'SF Mono', 'JetBrains Mono', monospace` | Code, technical values |


### Font Weights

| Token | Value | Use |
|-------|-------|-----|
| `core.font.weight-light` | 300 | Decorative, large display |
| `core.font.weight-regular` | 400 | Body text, paragraphs |
| `core.font.weight-medium` | 500 | Subtle emphasis |
| `core.font.weight-semibold` | 600 | Headings (moderate mode) |
| `core.font.weight-bold` | 700 | Strong emphasis |
| `core.font.weight-extrabold` | 800 | Headings (strong mode) |


### Type Scale

Sizes are parametric (scale hierarchy parameter `sh`). Values shown at default `sh = 1.0`. Fixed sizes stay constant regardless of `sh`; scaled sizes grow/shrink with the parameter.

| Token | Category | Size | Fixed | Line Height | Use |
|-------|----------|------|-------|-------------|-----|
| `semantic.typography.heading.2XL` | Heading | 36px | no | 1.0× | Page titles, hero headlines |
| `semantic.typography.heading.XL` | Heading | 32px | no | 1.0× | Section headings |
| `semantic.typography.heading.L` | Heading | 28px | no | 1.0× | Subsection headings |
| `semantic.typography.heading.M` | Heading | 24px | no | 1.0× | Card titles, group headers |
| `semantic.typography.heading.S` | Heading | 20px | yes | 1.0× | Small headings, labels |
| `semantic.typography.interface.XL` | Interface | 20px | yes | 1.1× | Large UI elements |
| `semantic.typography.interface.L` | Interface | 18px | yes | 1.1× | Navigation, prominent labels |
| `semantic.typography.interface.M` | Interface | 16px | yes | 1.1× | Default body text, buttons |
| `semantic.typography.interface.S` | Interface | 14px | yes | 1.1× | Secondary labels, metadata |
| `semantic.typography.interface.XS` | Interface | 12px | yes | 1.1× | Captions, badges, timestamps |
| `semantic.typography.paragraph.XL` | Paragraph | 20px | yes | 1.4× | Lead paragraphs, intros |
| `semantic.typography.paragraph.L` | Paragraph | 18px | yes | 1.4× | Featured body text |
| `semantic.typography.paragraph.M` | Paragraph | 16px | yes | 1.4× | Default long-form reading |
| `semantic.typography.paragraph.S` | Paragraph | 14px | yes | 1.4× | Footnotes, fine print |
| `semantic.typography.paragraph.XS` | Paragraph | 12px | yes | 1.4× | Legal text, disclaimers |
| `semantic.typography.caps.L` | Caps | 14px | yes | 1.1× | Prominent category labels |
| `semantic.typography.caps.M` | Caps | 12px | yes | 1.1× | Section labels, tab headers |
| `semantic.typography.caps.S` | Caps | 10px | yes | 1.1× | Overlines, label categories |
| `semantic.typography.code.M` | Code | 16px | yes | 1.4× | Code blocks, token values |

**Category semantics:** Interface = compact UI (buttons, labels, nav). Paragraph = reading text (articles, descriptions). Caps = uppercase labels (always letter-spaced). Code = monospaced technical content.


### Line Heights

Line-height values are unitless multipliers of font-size. Never use px or %.

| Token | Value | Use |
|-------|-------|-----|
| `core.font.lineHeight-heading` | 1.0 | Headings — tight |
| `core.font.lineHeight-interface` | 1.1 | UI elements — compact |
| `core.font.lineHeight-paragraph` | 1.4 | Reading text — comfortable |


### Paragraph Spacing

Paragraph spacing is parametric (`ph`). At default `ph = 1.0`, the space between paragraphs equals `16 × ph` = 16px. This is separate from line height and only applies between block-level text elements.



## 4. Layout & Spacing

Spacing scale, sizing, radius, borders, and grid system. All values snap to a 4px grid.


### Spacing Scale

Parametric via `space_factor` (`sf`). Values shown at default `sf = 1.0`.

| Token | Name | Value | Use |
|-------|------|-------|-----|
| `semantic.space.*.0` | None | 0px | No spacing |
| `semantic.space.*.2XS` | 2XS | 4px | Tight internal gaps |
| `semantic.space.*.XS` | XS | 8px | Icon gaps, inline spacing |
| `semantic.space.*.S` | S | 12px | Related element spacing |
| `semantic.space.*.M` | M | 16px | Default component padding |
| `semantic.space.*.L` | L | 24px | Group spacing, card padding |
| `semantic.space.*.XL` | XL | 32px | Section spacing |
| `semantic.space.*.2XL` | 2XL | 48px | Major section breaks |
| `semantic.space.*.3XL` | 3XL | 64px | Page section separators |

The `*` wildcard represents the semantic category: `padding`, `gap`, `margin-x`, `margin-y`, `article`.

For page-level whitespace beyond 3XL, use **layout tokens** rather than extending the scale.


### Sizing Scale

Parametric via `dimension_factor` (`df`). Values shown at default `df = 1.0`.

| Token | Name | Value | Use |
|-------|------|-------|-----|
| `semantic.size.icon.S` | Icon S | 16px | Inline icons |
| `semantic.size.icon.M` | Icon M | 24px | Default icons |
| `semantic.size.icon.L` | Icon L | 32px | Prominent icons |
| `semantic.size.icon.XL` | Icon XL | 48px | Feature icons, illustrations |
| `semantic.size.action.XS` | Action XS | 24px | Compact touch targets |
| `semantic.size.action.S` | Action S | 32px | Small buttons, tags |
| `semantic.size.action.M` | Action M | 40px | Default buttons, inputs |
| `semantic.size.action.L` | Action L | 48px | Prominent actions |
| `semantic.size.action.XL` | Action XL | 56px | Hero CTAs |


### Radius Scale

Parametric via `roundness_factor` (`rf`). The values below must match the exported values in `core.tokens.json` / `semantic.tokens.json` exactly — the table is populated from the current export, not from a fixed snap grid.

| Token | Name | Value | Use |
|-------|------|-------|-----|
| `semantic.radius.none` | None | 0px | Sharp corners |
| `semantic.radius.S` | S | 2px | Subtle rounding (badges) |
| `semantic.radius.M` | M | 4px | Buttons, inputs, chips |
| `semantic.radius.L` | L | 8px | Cards, modals |
| `semantic.radius.XL` | XL | 16px | Large containers |
| `semantic.radius.2XL` | 2XL | 32px | Hero sections |
| `semantic.radius.pill` | Pill | 9999px | Pills, tags, full-round |


### Border Width Scale

Parametric via `border_width` multiplier (`bw`). The values below must match the exported values in `core.tokens.json` / `semantic.tokens.json` exactly — including sub-pixel values (e.g. 0.5px, 1.5px, 2.25px). Do not round.

| Token | Name | Value | Use |
|-------|------|-------|-----|
| `semantic.border.width.none` | None | 0px | No border |
| `semantic.border.width.XS` | XS | 0.5px | Hairline dividers |
| `semantic.border.width.S` | S | 1px | Default borders |
| `semantic.border.width.M` | M | 1.5px | Emphasized borders |
| `semantic.border.width.L` | L | 2px | Strong outlines |
| `semantic.border.width.XL` | XL | 2.5px | Accent borders |
| `semantic.border.width.2XL` | 2XL | 3px | Heavy decorative borders |


## 5. Depth & Elevation

Shadow system and surface hierarchy. Shadow colors use foundation dark.clear at computed alpha levels (6%–18%) for consistent depth across themes.

| Token | Level | Shadow | Use |
|-------|-------|--------|-----|
| `semantic.elevation.0` | 0 | none | Flat surfaces |
| `semantic.elevation.1` | 1 | `0 1px 3px` @12%, `0 1px 2px` @8% | Cards, raised panels |
| `semantic.elevation.2` | 2 | `0 4px 16px` @12%, `0 1px 4px` @6% | Popovers, dropdowns |
| `semantic.elevation.3` | 3 | `0 8px 32px` @14%, `0 2px 8px` @8% | Modals, dialogs |
| `semantic.elevation.4` | 4 | `0 16px 48px` @18%, `0 4px 16px` @10% | Toasts, notifications |
| `semantic.elevation.focus` | Focus | `0 0 0 3px` focus color | Focus indicator ring |


### Surface Hierarchy

| Level | Surface | Elevation | Token |
|-------|---------|-----------|-------|
| Base | Page background | 0 | `foundation.themed.base-clear` |
| Raised | Cards, panels | 1 | `foundation.themed.base-cloudy` |
| Floating | Dropdowns, popovers | 2 | `foundation.themed.base-clear` |
| Overlay | Modals, dialogs | 3 | `foundation.themed.base-clear` |
| Toast | Notifications | 4 | `foundation.themed.base-clear` |



## 6. Construction Rules

Mandatory structural requirements for every design output, regardless of medium, scope, or context.


### Reuse Mandate

Before creating anything, inventory what already exists:

1. **Variables/Tokens**: If Figma Variables, CSS custom properties, or token files exist — use them
   exclusively. Never hardcode any value. Never define core or semantic tokens, those collections are complete. Other optional collections (for e.g. breakpoints, density, typography, colors) may be edited if necessary. Always align with the user before CRUD-operations on tokens.
2. **Components**: If a component library exists — use existing components. Only create new
   components when no suitable match exists. Check the library before building from primitives. Always align with the user before CRUD-operations on components.
3. **Screens/Flows**: If screens or flows already exist in the project — follow their patterns.
   Match existing layout structures, spacing rhythms, and component usage. New screens should
   look like they belong to the same product.


### Layout Structure

All layout must use **structured layout** — the tool's native layout engine (Auto Layout in
Figma, Flexbox/Grid in CSS/HTML, layout containers in Pencil, etc.). The term "structured
layout" is used throughout; apply the equivalent mechanism in the active tool.

- **Containers always**: Every element lives inside a layout container. No loose shapes, text
  nodes, or ungrouped elements.
- **Structured layout always**: Every container uses the tool's layout engine. No manual
  positioning inside layout containers.
- **Full spectrum**: Structured layout covers everything from the outermost page container down
  to the smallest component — nested layouts at every level.
- **Direction**: Choose horizontal, vertical, wrap, or grid based on content flow.
- **Spacing**: Use gap tokens from Section 5. No manual offset hacks.
- **Padding**: Use padding tokens from Section 5. Apply to container elements.
- **Constraints**: Elements that must escape structured layout (floating buttons, overlays,
  fixed headers, sticky footers) use explicit positioning constraints — set to top-left,
  top-right, bottom-left, bottom-right, center, or stretch. Never leave positioning at default
  when position matters.


### Professional Quality

The output must look like a shipped product, not a wireframe or rough prototype:
- Pixel-perfect alignment (structured layout handles this automatically)
- Correct typography hierarchy (heading > subheading > body > caption)
- Realistic placeholder text — not "Lorem ipsum" (exception: long-form body where content is
  not the focus)
- Proper image placeholders with aspect ratios matching intended content
- All interactive states considered (default, hover, focus, active, disabled)
- Edge cases handled (empty states, loading states, error states, overflow)
- Consistent spacing throughout — no "eyeballed" gaps


### Token Usage Hierarchy

Always reference the highest available token layer. On setup, inspect which token collections
exist and use the topmost level available:

1. **Semantic tokens** — Use for everything: layout, spacing, typography, colors on screens
   and pages (e.g. `semantic.space.padding.M`, `foundation.themed.base-clear`). Semantic
   tokens are the default layer for all work — no scoped component-token layer exists in
   this system.
2. **Optional collections** — Some projects have additional token sets (e.g. density,
   breakpoints, color overrides). When these exist, they override the semantic defaults
   for their scope. Check what's available before starting.
3. **Core tokens** — Never reference directly. Core tokens are primitives consumed by the
   semantic layer. If you find yourself writing `core.color.brand-a.500`, use the semantic
   equivalent `brand-a.themed.base` instead.



## 7. Do's and Don'ts

Design guardrails and anti-patterns.


### Do

- Use semantic tokens exclusively — never reference core tokens directly
- Maintain minimum AA contrast (4.5:1) for all text on backgrounds
- Use `contrast-primary` for headings and body text, `contrast-secondary` for labels and metadata
- Keep spacing on the 4px grid — all spacing values snap to multiples of 4
- Use elevation tokens for depth — never hardcode box-shadow values
- Let the parametric system calculate values — adjust parameters, not individual tokens
- Use `foundation.themed` tokens for theme-aware surfaces
- Use `foundation.inverted` for contrast sections (e.g. dark footer on light page)
- Use structured layout for every container — no manual positioning
- Use existing variables/components before building new ones — align with the user on all CRUD-operations on variables and components
- Set explicit positioning constraints on elements that break out of structured layout
- Design for all relevant states (default, hover, focus, active, disabled, empty, loading, error)


### Don't

- Don't hardcode hex values — always reference semantic tokens
- Don't mix font families within a single text block
- Don't use more than 3 elevation levels on a single screen
- Don't use `contrast-tertiary` for readable text — it's only for placeholders and disabled states
- Don't combine multiple brand colors on a single interactive element
- Don't use the spacing scale for page-level layout gaps above 3XL — use layout tokens instead
- Don't skip the themed/inverted pattern — even single-theme products should use it for future-proofing
- Don't leave containers without structured layout (except canvas-level wrappers)
- Don't create new components when an existing one can be configured via variants/props
- Don't reference core tokens directly — always use the semantic layer equivalent



## 8. Design Scope

This DESIGN.md applies to any design context. The same tokens, construction rules, and quality
bar apply whether the task is:

| Context | What it means | Componentize? |
|---------|---------------|---------------|
| Design from image/reference | Reproduce the visual style using these tokens | Discuss with user |
| Single component | Build one component following construction rules | Yes |
| Atoms | Smallest elements (button, input, badge, icon) | Always — these are components by definition |
| Molecules | Simple groups of atoms (search bar, form field with label) | Very likely — discuss with user |
| Organisms | Complex sections (header, card grid, sidebar navigation) | Maybe — discuss with user |
| Templates / Pages | Full screen layouts composed of organisms | No — compose from existing components |
| Flows | Multi-screen sequences with transitions | No — compose from existing components |

**Componentization rule**: Not everything needs to become a reusable component. Atoms are
always components. Molecules very likely. Organisms and above: discuss with the user whether
reuse is expected. Templates, Pages, and Flows are compositions — they consume components but
are not components themselves.



